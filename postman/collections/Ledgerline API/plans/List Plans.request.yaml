$kind: http-request
url: "{{baseUrl}}/plans?limit=20&starting_after=string"
method: GET
headers:
  Accept: application/problem+json
queryParams:
  - key: limit
    value: "20"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "default": 20,
            "minimum": 1,
            "maximum": 100
          }
        }
      }
  - key: starting_after
    value: string
    $type: string
body:
  type: text
  content: ""
examples: ./.resources/List Plans.resources/examples
order: 1000
